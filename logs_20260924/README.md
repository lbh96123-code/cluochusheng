# AD 选技助手 · 实战日志（2026-09-14 ~ 09-24，已脱敏）

插件自动上传到服务器的全部日志包（607 个），按玩家整理、去重后打包。

- `玩家X.tar.xz`：该玩家的全部会话日志 `ad_*.log` + 选技观测轨迹 `trace_*.jsonl`（同一份日志在多个上传包里重复出现时，只保留最完整的一份）。
- `索引.csv`：每个上传包一行：日期、时间、玩家代号、触发原因（draft_end 一局选完 / abnormal 选技超 3 分钟 / nolock 锁不上池子 / manual 手动 / prevcrash 上次会话没正常退出）、插件版本、包内文件。

脱敏：安装号 → 玩家代号（同一人的多个安装号合并成一个代号）；`C:\Users\xxx` → `C:\Users\某用户`；截图全部不含；服务器端的 IP 记录不含。日志和轨迹里没有游戏昵称。

读法：
- 日志每行 `[时刻] 标签 内容`。常用标签：`pool` 锁池/池子、`track` 谁拿了什么、`board` 各座位当前阵容、`advice` 推荐（`└ 全候选` 一行是全部候选的分数）、`turn` 我的回合小结、`cap` 截屏、`hb` 心跳（v1.39 起）。
- 轨迹 `trace_*.jsonl`：第一行 `head` 含池子（`pool.heroes` / `pool.skills`）；之后 `f` 是完整识别帧（每格亮度、面板、当前选人等），`s` 是快扫帧。回放工具见插件源码 `tools/audit_taken/`。

还原某局完整选人顺序的做法见 `../algo_20260924/experiments/`（`real_games.json` 就是这样从日志里还原出来的）。
