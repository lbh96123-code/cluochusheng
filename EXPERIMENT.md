# 实验契约(所有新方法必读)

目标:在 **只看 draft** 的前提下,把 AD 对局的胜负预测做得更准。

## 一、绝对不能违反的三条

### 1. 输入只有 ID

唯一允许的数据是 `data_pure.npz` 里的 `seats` (n,10,5) int16 ——
10 个座位,每座位 5 件东西在 `ids[]` 里的下标(1 个英雄本体 + 4 个技能,**顺序不固定**;
英雄本体在 `ids[]` 里的值是负数,可以用 `ids[seats] < 0` 判定)。
前 5 个座位是一方,后 5 个是另一方。

**禁止读取任何其它文件**,包括但不限于:
`data.npz`(含 pick order)、`feats.npz`、`wrfeat.npz`、`/tmp/wr_*.json`、
`matches*.jsonl`、`public/ad_data.js`、`model_full.npz`、`fp_*.npz`。

也就是说:不许用 windrun 的胜率/选取率/平均顺位/两两组合表/三连表/阿哈/时长分档,
不许用 pick order,不许用玩家 rating,更不许碰任何赛后信息(击杀/经济/时长/物品)。
一切都得从 640 个 ID 和胜负标签里自己学。

> 你**可以**使用从 `seats` 自己统计出来的任何量(共现次数、频率、聚类…),
> 只要它是从训练段算出来的。用到验证段或测试段去统计就是泄露。

### 2. 反对称

模型必须提供 `score(S) -> z`,S 是 (B,10,5),z 是 (B,)。
把前 5 座位和后 5 座位**整体对调**,z 必须**变号**。

`bench.evaluate()` 会自动做这个自检,`max|z(A,B)+z(B,A)| >= 1e-3` 直接判结果作废。

最简单的满足方式:`z = f(前5座位) - f(后5座位)`,f 共享参数。
先手优势(数据里约 53.8%)由 evaluate() 单独拟合一个标量截距,**不要你处理** ——
模拟器里没有天辉/夜魇,那个截距最后会被扔掉。

### 3. 切分固定

```
训练 [0, 0.80)   验证 [0.80, 0.85)   测试 [0.85, 1.0)
```

数据已按开局时间排序,**不许重新洗牌**。验证集只能用于早停和选超参,
测试集只在最后调一次 `bench.evaluate()`。用测试集调任何东西都是作弊。

## 二、怎么写

```python
import bench
seats, y, ids, (tr, va, te) = bench.load()
# ... 你的训练,只用 seats[tr] 和 y[tr],用 seats[va]/y[va] 早停 ...
def score(S):          # S: (B,10,5) int -> (B,) float,反对称
    ...
r = bench.evaluate('你的方法名', score, note='超参...')
import json; json.dump(r, open('result_<你的方法名>.json','w'))
```

`bench.evaluate` 会打印一行 `RESULT<TAB>名字<TAB>AUC=...<TAB>logloss=...`,
外加校准表和反对称自检。**以 logloss 为主要指标**(游戏里要显示概率,不是硬分类),
AUC 作为参考。

## 三、机器约束(硬性)

这台机器 **2 核 / 8 GB**,而且跑着**实盘交易进程,绝对不能拖垮**。

- 单个实验峰值内存 **≤ 2 GB**,跑之前想清楚张量大小。
  `e[seats]` 这种 (466k,10,5,32) 的整体索引 = 3 GB,**必须分批**。
- 所有训练命令加 `nice -n 15`。
- **不许并行跑训练**,一次只能有一个。
- 单个实验挂钟时间 **≤ 25 分钟**。跑不完就缩小 epoch 或维度,别硬扛。
- 没有 GPU,没有 torch/tensorflow。可用:numpy 1.26、scipy 1.13、sklearn 1.6。
  需要别的包先问,不要自己 pip install(可能污染实盘环境)。

## 四、当前靶子

| 模型 | AUC | logloss |
|---|---|---|
| 常数(先手胜率) | 0.5000 | ~0.690 |
| **纯 ID 基线**:640 维主效应 + 640×32 座位内低秩协同(FM) | 见 `result_baseline_pure_d32.json` | |

参考(**这些用了 windrun 外部表,不满足本次约束,只作上限参考**):
46.6 万局上,16 块 windrun 特征 + 主效应 + 座位嵌入 = AUC 0.7587 / logloss 0.5811。

## 五、已经试过并判负的(别重复)

| 方向 | 结论 |
|---|---|
| 跨队克制(双线性 ⟨U_A,V_B⟩−⟨U_B,V_A⟩) | 15.5万/23.9万/46.6万 三次全负,每次都比不加更差 |
| 座位层 DeepSets(池化后过 MLP) | 0.7394/0.7486/0.7475,全部不如纯双线性 |
| 队伍层非线性聚合(开方饱和、只取最强 8 件) | 0.6970 / 0.6584,远差于线性 |
| 中介法(先拟合战力指标再映射胜负) | 同特征下 AUC 完全相等;高维下反而更差 |
| 高共现组合放自由参数 θ_ij | 23.9万 +0.0005,46.6万 还是 +0.0005 |
| 按 pick order / 强度位次重新加权 | 与简单加总相关 0.9977,无增益 |

## 六、已知的结构事实(可以当线索)

- 座位内协同里,**本体×技能** 占方差 37.2%,**技能×技能** 占 52.8%;
  摘掉本体参与协同会掉 0.0047 AUC —— "技能装在谁身上"是真实且重要的。
- 学出来的协同矩阵**高度低秩**:第 1 主轴在 23.9 万局时占 79%,46.6 万局时降到 72%。
  两根轴分别近似是"身板好不好用 × 技能挑不挑身板"和"普攻流 ↔ 法系爆发"。
  剩下的方差里应该还有更细的结构没被挖出来。
- 现有模型全都在**第 2~5 轮就早停**(共 12~24 轮)。这可能是优化/正则没调好,
  而不只是数据不够 —— 值得单独查。
- 组合共现:C(513,2)=131,328 对里,中位共现 151 次,≥500 次的只占 5.29%。
---

## 七、铁律:训练只许用游戏内 ID(2026-09-04 定)

**训练输入 = 10 座位 × 5 件东西的编号,仅此而已。**

以下一律**不得进入训练**,只能用作**事后观测 / 评测指标**:

| 类别 | 例子 |
|---|---|
| 英雄元数据 | 主属性(力/敏/智)、近战远程、`dc_heroes.json` 任何字段 |
| 技能元数据 | `dc_abilities.json` 的 dmg_type / behavior / bkbpierce / dispellable |
| 赛后统计 | gpm / xpm / 补刀 / 伤害 / 治疗 载荷,windrun 的胜率 / 选取率 / 顺位表 |
| 人工构造 | 手工挑的轴、白名单、"已确认效应"清单 |

**理由**:模型该展示的克制/配合特性必须**完全由训练自己拟合出来**,之后我们再拿标签去查
"学到没有"。标签一旦进训练,就分不清是模型学的还是人喂的,结论作废。

**现状**:线上 `model_plan15_clr0.25` 的 15 条克制轴里,4 条属性计数 + 3 条赛后浓度
**不干净**,属历史遗留。干净替代已验证:`PC16+B`(e 的前 16 主成分、本体与技能分开求和),
测试 0.57707,对线上配对 t=−0.09。

**观测怎么用**:例如用 `dmg_type` 建"魔法伤害技能"组(253 件),测出法术反制 z=+2.71(排 9/513)、
腐蚀皮肤 +2.34、脉冲新星 −2.55、幽魂护罩 −3.25 —— 这类结果只写进报告,不进模型。
