# AD 数据源侦察 + 胜负拟合器方案

> 2026-08-29。目标:给 `ad-draft` 选技模拟器做一个"选完之后判谁赢"的模型。
> 结论先说:**windrun 是唯一值得用的源,纯 draft 的预测天花板是 AUC 0.72~0.75 / 准确率 65~68%,
> 而且用它的公开胜率表零训练就能拿到 0.724 —— 先把这个上了,再考虑自己训。**

---

## 一、数据源清点

### 1. windrun.io —— 主力源(免 key)

⚠️ Cloudflare 只放行 **curl 的 TLS 指纹**。node 的 `fetch`、python 的 `requests` 都会吃到
"Just a moment..." 挑战页。所有抓取一律走 curl 子进程(`crawl_stream.py` 就是这么干的)。
必带 `User-Agent`(浏览器串)和 `Origin: https://windrun.io`,不然 403。

**聚合表** `https://api.windrun.io/api/v2/…`,全部支持 `?patch=7.41b` 回溯历史补丁:

| 端点 | 内容 | 规模 / 样本量 |
|---|---|---|
| `/abilities` | 单体胜率、选取率、**平均被选顺位**及标准差 | 640 条(513 技能 + 127 英雄本体);当前补丁每条 3.5 万~7.4 万样本 |
| `/ability-pairs` | 两两组合胜率 | top **7500** 组,最小样本 672 |
| `/ability-triplets` | 三连组合胜率 | top **10000** 组,最小样本 258(只有 20% 达到 500) |
| `/ability-high-skill` | 全体 + **高分段**两份 | 高分段中位样本 4310 |
| `/ability-hero-attributes` | 技能装在 力/敏/智 载体上的分组胜率 | 527 KB |
| `/ability-aghs` | 有无阿哈的分组胜率 | 242 KB |
| `/ability-shifts` | 每技能对 K/D/A、GPM、XPM、伤害的偏移量 | 282 KB |
| `/abilities-by-time` | 按对局时长分段的胜率 | 8 个时长档 |
| `/heroes`, `/heroes/historic` | 英雄胜率(当前 / 全部 61 个补丁) | — |
| `/static/patches` | 每个补丁的总局数、解析数、起始时间、天辉胜率 | 61 个补丁 |
| `/leaderboard/{global\|europe\|americas\|china\|sea}` | top 100 玩家 + rating | — |

**原始对局**(这才是训练数据):

- `/api/v2/matches/{matchId}` —— 单局
- `/api/v2/players/{steamId}/matches?page=N` —— **一次 30 局全字段**,page 0 是最新

每局给到:

```
picks         50 手(abilityId + pickOrder)     ← 完整选技顺序
ignoredSpells 10 件池子里没被选走的            ← picks ∪ ignored = 60 件池子,可完整还原
radiant/dire  各 5 人: hero, abilities[5](含英雄本体的负 id),
              steamId, rating(windrun 自己的分), 生涯 winLoss,
              kills/deaths/assists/gpm/xpm/heroDamage/items  ← 这一行是赛后信息,建模禁用
radiantWin, duration, patch, gameStart, delta(rating 变动)
```

**样本量**:

- 当前补丁 **7.41d(2026-06-05 起)78.2 万局**
- 上一个 7.41b 79.5 万局、7.40c 73.4 万局
- 全站 61 个补丁累计 **2594 万局**,最早回到 2020-06-28
- 天辉胜率常年 **53.0%~54.0%**(先手优势,模拟器里必须扣掉)

**实测抓取速度**(单线程 curl):

```
2.5 req/s,玩家翻页每个请求净得 14 局去重后新对局,每局 11 KB
→ 20 万局 ≈ 95 分钟 / 2.1 GB
```

### 2. OpenDota —— 用来做无偏采样

- `/api/explorer` 可以直接跑 SQL:`select match_id from public_matches where game_mode=18 …`
  (**game_mode 18 = ability draft**)。实测 1M 个 match_id 区间里有 58.8 万条公开局、
  其中 **5367 局 AD(0.91%)**。查询要按 `match_id between` 走索引,按 `start_time` 会超时。
- 全球 AD 产量:近 6 小时 1620 局 → **约 6500 局/天**(公开局)。
- `/matches/{id}` 有 `ability_upgrades_arr` 能反推选到的技能,但**没有池子、没有 pick 顺序**,
  且限速 60/分、2000/日。所以 OpenDota 只用来**产无偏的 match_id 种子**,
  真正的数据还是回 windrun 取。

> 为什么需要它:直接从 windrun 排行榜起爬会偏向高分玩家;从随机 match_id 起爬再滚雪球,
> 分布接近真实人群。

### 3. STRATZ —— ❌ 不支持 AD

token 有效,普通局(`ALL_PICK_RANKED`)正常返回,但所有 game_mode 18 的对局
一律 `{"data":{"match":null}}`。STRATZ 根本不收 AD,放弃。

### 4. 其他

- **datdota.com** —— 和 windrun 同一批人,只做职业赛;英雄头像 CDN 在这(`cdn.datdota.com/images`)。
- **HGV tango**(tango.highgroundvision.com)—— 有 AD 页面但没公开 API。
- **Ability Draft Plus**(tiarinhino.com)—— 现成的游戏内浮层工具。它的 MobileNetV2
  只用来**认技能图标**,数字全部来自 windrun 公开 API,**本身没有胜负模型**。
  也就是说这块目前没人做,不用担心重复造轮子。
- **joefkelley.com/765, /788**(2015)—— 22 万局的描述统计,只列了单体和两两胜率排行,
  没有建模。有一条教训值得抄:他明确指出 pair 胜率在样本 < 50 时全是噪声。

### 5. 关于 OMG

Dota 官方 `game_mode` 里**没有 OMG**(18 = ability draft,15 = custom)。
OMG 是 arcade 自定义游戏,Valve 不开放它的统计接口,OpenDota / windrun / STRATZ 都没有数据。
想要只能自己开房收集 —— **建议放弃**。

---

## 二、实测:预测天花板在哪

在 **11307 局 7.41d 真实对局**上,按时间切分(旧 75% 训练 / 新 25% 测试,不做随机切分):

| 模型 | AUC | logloss | 准确率 |
|---|---|---|---|
| 常数(天辉 54.3%) | 0.500 | 0.690 | 0.543 |
| **windrun 技能胜率直接相加(零训练)** | **0.706** | 0.621 | 0.653 |
| 自己学 640 维主效应(仅 1.1 万局) | 0.676 | 0.644 | 0.626 |
| 只用玩家 rating 差(纯实力,无 draft) | 0.701 | 0.637 | 0.649 |
| **技能胜率和 + 座位内两两组合协同** | **0.724** | 0.609 | — |
| 上面再加实力差 | 0.766 | 0.575 | — |

三条结论:

**1. 别自己从零学主效应。** windrun 的胜率是 78 万局算出来的,我用 1.1 万局学出来的
(0.676)反而更差。正确姿势是把 windrun 的数字当先验/特征,只学**偏移量**。

**2. "强者选强技能"这个混淆没想象中严重。** 技能分与 rating 差的相关系数只有 **0.199**;
把 rating 当控制变量后,技能分的系数只从 4.14 掉到 3.88(−6%)。
在实力接近的子样本里做自然实验:

```
|Δrating| < 100 (1441 局): 技能分 AUC 0.6919
|Δrating| <  50 ( 880 局): 技能分 AUC 0.7116
|Δrating| <  25 ( 448 局): 技能分 AUC 0.7095
```

实力拉平之后技能分照样有 0.71。**所以拿公开胜率做纯 draft 模型是站得住的**,
不需要搞复杂的因果矫正。

**3. 纯 draft 的现实天花板 ≈ AUC 0.72~0.75 / 准确率 65~68%。**
剩下的是操作、分路、掉线、心态,不可预测。
**如果你的模型跑出 0.9,那一定是漏进了赛后信息**(kills / gpm / duration / items)。

---

## 三、建议怎么做

### 硬约束:反对称

```
logit = f(队A) − f(队B),f 共享参数
```

这样交换左右两边概率自动翻转,不会出现"左边天生更容易赢"。
天辉那 53~54% 的先手优势在模拟器里没有对应物,**训练时当截距学出来、推理时扔掉**。

### 分四层,按性价比排

**L0 —— 免费层(今天就能上,零训练)**

```
score = Σ (winrate_i − 0.5)                        i 遍历 25 件(5 英雄本体 + 20 技能)
      + Σ (pairWinrate_ij − winrate_i − winrate_j + 0.5)   座位内 C(5,2)=10 对
p(左赢) = sigmoid(4.45 · 第一项 + 3.24 · 第二项)     ← 系数已在 1.1 万局上标定
```

第二项是"超额协同",把两个技能各自本来就强的部分减掉,剩下才是组合价值。
**AUC 0.724,一段 JS,不需要任何训练数据。** 这已经比大多数人肉眼判断准。

**L1 —— 残差修正层(需要 10~20 万局)**

用自己抓的对局学一个**残差模型**:目标是 `y − L0预测`,特征是 640 维差分指标
(某件东西在左队 +1、右队 −1)。L2 正则给强一点(C ≈ 0.03~0.1)。
这一步专治 windrun 单体胜率的选择偏差 —— 好技能总跟好技能一起被选走,
胜率被互相抬高了,残差层负责把它拉回"共同出现时的真实边际贡献"。

**L2 —— 协同层(真正值钱的,需要 ≥20 万局)**

因子分解机 / DeepSets,别用自由的 pair 参数:

- 每件物品一个 d=16~32 的向量 `e_i`
- 座位内协同 = `Σ_{i<j∈seat} ⟨e_i, e_j⟩` —— 640×32 ≈ 2 万参数,
  比 C(640,2) = 20 万个自由 pair 参数省两个量级,而且**对没见过的组合能泛化**
- 跨队克制 = 另一组向量 `u`,`Σ_{i∈A, j∈B} ⟨u_i, u_j⟩`(沉默 vs 法系那种)
- 结构:物品嵌入 → 座位内求和 + 小 MLP → 5 座位求和 = 队向量 → `logit = g(A) − g(B)`

每局提供 5 座位 × 10 对 = 50 个组合观测,20 万局 = 1000 万个,够喂这个规模。

**L3 —— 手工特征(补稀疏处)**

从 dotaconstants 拿每个技能的 `behavior` / `damage type` / `target`,做队级聚合:
控制技能数、AoE 数、位移数、魔法/物理伤害占比、被动主动比、大招数量。
这些在样本稀疏的地方比嵌入靠谱,而且**可解释** —— 结算页能写"左边缺控"而不只是一个数字。

windrun 的 `/ability-hero-attributes` 直接给了"这技能装在力量/敏捷/智力英雄上的胜率",
正好做**英雄-技能适配度**特征(近战身板配远程被动那种错配)。

### 训练纪律(这几条一定守)

1. **只用开局前可见的信息。** windrun 的对局 JSON 里有 kills / gpm / items / duration,
   一个都不能进特征 —— 那是赛后信息,会让 AUC 假涨到 0.9+。
2. **按时间切分,不做随机切分。** 补丁会漂移,同一个玩家的多局会串。
3. **训练时把 rating 差当控制变量,推理时置 0。** 已验证这样做 AUC 几乎无损
   (0.7055 → 0.7055),但能防系数被实力污染。
4. **只用当前补丁**;要更多样本就把 7.41b 一起放进来,加一个补丁哑变量。
5. **看 logloss 不看准确率。** 你要的是概率(结算页显示"左 63%"),不是硬分类。
   再补一条**校准曲线**:预测 60% 的那批是不是真的赢了 60%。

### 跟游戏怎么接

- 模型权重 = 640 个标量 + 640×32 嵌入 ≈ **100 KB JSON**,和 `ad_data.js` 一起发给浏览器,
  前端算 logit,**不需要服务端推理**。
- 我们的池子只有 12 英雄 / 60 件,模型对全集训练但推理只碰这 60 件,天然轻量。
- **顺带白送一个 bot**:同一个模型对每个候选算 Δlogit,就是选技推荐 ——
  可以拿来做 AI 对手,或者在结算页做"最优解复盘"。

### 暂时不做

- **按时长分层**(`/abilities-by-time` 提示不同技能在长短局强度不同)—— 是真的,
  但开局时你不知道会打多久,属于 v2。
- **高分段单独建模** —— 高分段样本只有全体的 1/15,先用全体。

---

## 四、目录里有什么

```
research/
├── crawl_stream.py    windrun 流式抓取(内存只留 matchId,边抓边落盘)
├── crawl.py           初版(全量进内存,已弃用 —— 8G 机器会 OOM)
├── baseline.py        M0/M1/M2 三档基线 + 实力控制
├── baseline2.py       技能分 vs 实力分的拆解 + 自然实验 + 组合表增量
├── matches.jsonl      12001 局(7.41d 11307 局)
└── matches_big.jsonl  15 万局,抓取中
```

复现:

```bash
# 1. 从 OpenDota 拿无偏种子(注意用 match_id between 走索引)
curl -sG --data-urlencode "sql=select match_id from public_matches \
  where match_id between 8968000000 and 8969000000 and game_mode=18 limit 400" \
  https://api.opendota.com/api/explorer > seed.json

# 2. 抓
python3 crawl_stream.py seed.json matches.jsonl 150000 7.41d

# 3. 评
python3 baseline2.py matches.jsonl
```

---

## 五、数据够不够 / 全量要多少空间

### 够,而且远超需要

windrun 全站 **2594 万局**(`lifetimeStats.totalGames` = 25,804,301),
现在以 **每周约 6.5 万局 / 每周 9 万独立玩家**的速度增长。

| 补丁族 | 局数 | 说明 |
|---|---|---|
| 全站 61 个补丁 | 25,943,277 | 2020-06 至今;2020 年的技能数值和现在完全是两回事 |
| 7.41 全族 | 1,758,796 | 7.41/a/b/d,同一大版本 |
| **当前 7.41d** | **782,528** | 2026-06-05 起,12 周 |

**学习曲线实测**(固定用最新 3929 局当测试集):

```
windrun 公开胜率表(零训练)                AUC 0.7052
自己学 640 维主效应:
   1,571 局 → 0.6206      以 windrun 打底再学偏移 → 0.6763
   3,928 局 → 0.6555                            → 0.6825
   7,857 局 → 0.6794                            → 0.6907
  15,715 局 → 0.6956                            → 0.6996
```

每翻倍涨约 +0.014 AUC。**自己训到约 3 万局才追平 windrun 的公开表,10 万局以上才能明显超过**;
L2 的嵌入层(640×32 ≈ 2 万参数)需要 20~50 万局。当前补丁 78 万局绰绰有余。

### 空间(实测,12001 局样本)

```
原始 JSONL     5.11 KB/局
整体 gzip      0.87 KB/局   (5.9x)
瘦身二进制      251 字节/局   (60 件池子 + 50 个技能归属 + 10 个 rating + 胜负 + 时长)
瘦身 + gzip     190 字节/局
```

| | 局数 | 原始 | gzip | 瘦身 | 瘦身+gz |
|---|---|---|---|---|---|
| 全站 61 补丁 | 25,943,277 | **126 GB** | 21.5 GB | 6.1 GB | 4.6 GB |
| 7.41 全族 | 1,758,796 | 8.6 GB | 1.5 GB | 421 MB | 319 MB |
| 当前 7.41d | 782,528 | 3.8 GB | 665 MB | 187 MB | 142 MB |
| **30 万局(建议)** | 300,000 | 1.5 GB | 255 MB | **72 MB** | 54 MB |

这台机器 `/` 只剩 20 GB,全量原始和全量 gzip 都放不下,只有瘦身二进制(6.1 GB)塞得进 —— 但没必要。

### 瓶颈是抓取时间不是空间

实测单线程 **2.5 req/s × 每请求净得 13 局 = 32 局/秒**:

| 目标 | 单线程耗时 |
|---|---|
| 30 万局 | **2.6 小时** |
| 7.41d 全部 78 万局 | 6.8 小时(算上后期去重损耗 10~15 小时) |
| 7.41 全族 176 万局 | 15 小时(实际一天多) |
| 全站 2594 万局 | **225 小时 = 9.4 天**(实际 2~4 周) |

覆盖率:一局 10 个玩家,访问 f 比例玩家能覆盖 1−(1−f)¹⁰ 的对局 ——
要拿某补丁 95% 的对局得访问约 26% 的玩家。

好消息:玩家翻页天然偏最新,实测抓下来 **94.2% 是当前补丁**,不用额外过滤;
翻页深度没有上限(测到 page 60 仍返回 30 条)。

**结论:抓 30 万局当前补丁(2.6 小时 / 72 MB)。** 全量既没用又要 9 天 + 126 GB,
还会把一个爱好者站点的 API 打爆。真要更多就把 7.41b 一起抓,别再往前。

---

## 六、"直接拟合胜负"还是"两侧各算胜率再比"?(实测对拍)

问题:选技只是游戏的一部分,直接拿 0/1 胜负当目标是不是太糙?
是不是该两侧各算一个战力/胜率,再看两者的关系?

### 先澄清:结构上这两个是同一件事

```
p(左赢) = σ( f(左队) − f(右队) )
```

这就是"两侧各算一个强度分再比较",学名 Bradley-Terry。我方案里的反对称约束本来就是它。
分歧不在"要不要两侧各算分",而在 **f 长什么样**,以及**用什么当训练目标**。

### `Σ胜率` 确实错,错在四个地方

1. windrun 的是**边际胜率** —— 每个技能都是在"和别的强技能一起被选"的条件下统计的,
   相加等于把同一份共因重复计入
2. 概率不能相加,要加也得在 logit 空间加
3. 没有饱和 —— 5 个控制技能不等于 1 个的 5 倍
4. 没有位置结构 —— 同一个技能在大哥位和辅助位价值不同

正确做法是每件物品一个**联合估计**的 logit 贡献,而不是它的边际胜率。
`Σ胜率` 只配当地板基线(它的价值是零训练就有 0.70,用来验证后面的模型没写错)。

### 中介法(draft → 战力指标 → 胜负)实测:没有增益

21,314 局,训练 17,051 / 测试 4,263:

```
路线 A  draft ──────────────► 胜负            AUC 0.7252
路线 B  draft ──► 净GPM差 ──► 胜负            AUC 0.7253
        draft ──► 净XPM差 ──► 胜负            AUC 0.7253
        draft ──► 击杀差  ──► 胜负            AUC 0.7247
        draft ──► 四个一起 ──► 胜负            AUC 0.7252
```

**完全相等**,这是数学必然:同一组特征、同一个线性索引,第二段只是个单调变换,AUC 不变。
中介法不会凭空多出信息。

再测"高维下连续目标是不是更省数据"(学 640 维物品强度,同一测试集比 AUC):

```
训练局数     直接学胜负(逻辑回归)    学战力指标(岭回归)      差
 1,736            0.6254            0.6203        −0.0051
 4,340            0.6553            0.6517        −0.0036
 8,680            0.6747            0.6704        −0.0044
17,360            0.6929            0.6834        −0.0096
```

连续目标反而**每一档都更差**。原因:GPM/XPM 是**赛后**量,被胜负本身污染
(赢的一方自然 farm 多),它不是更干净的 draft 信号,而是更脏的中介。

> 唯一可能有用的变体是 **GD@20(20 分钟经济差)** —— 那是"draft 已经兑现、胜负未定"的时点,
> 污染小得多。但它要 OpenDota 的 parsed match(`radiant_gold_adv`),限速 2000/日。
> 值得拿 2 万局单独验一次,不值得当主路线。

### 天花板的定量拆解

```
用赛后真实战力指标预测胜负          AUC 0.9974   ← "这局怎么打的"几乎完全决定胜负
draft 能解释战力指标的方差 R²        0.13 ~ 0.16   ← draft 只占这么点
draft-only 实测                    AUC 0.7252
```

所以"选技只是游戏的一部分"是**天花板问题**,不是目标变量问题。
推论:模型必须**主动往 50% 收缩**,它输出的不是"谁会赢",
而是"这套技能给你多少胜率倾斜"。

### 那该往哪个方向复杂化?(实测,22,480 局)

**队伍层面的聚合形式 —— 线性就够,别硬上非线性:**

```
线性相加 Σ(胜率−0.5)              AUC 0.7024
饱和聚合 Σ sign·√|·|              AUC 0.6970   ← 更差
只算最强的 8 件                    AUC 0.6584   ← 差很多
三种一起让模型自己挑                AUC 0.7028   ← 没长进
```

**座位结构确实有信息 —— 这是个干净的安慰剂检验:**

```
线性 + 真实座位内组合              AUC 0.7262   (+0.024)
线性 + 打散重组的假座位(对照)       AUC 0.7042   (+0.002)   ← 安慰剂
两个都放进去                       AUC 0.7260              ← 假的那个加不出东西
```

把同样 25 件东西随机重新分成 5 组,协同信号就消失了。
说明模型学到的是**"哪两个技能落在同一个英雄身上"**,而不是"哪些技能在同一队里共存"。
→ **L2 的嵌入必须按座位聚合,不能按队伍聚合。**

### 修正后的结论

- 目标变量:**就用 0/1 胜负**。换连续中介无益甚至有害(除非拿到 GD@20 再说)。
- 结构:两侧各算分再比 —— 本来就是这么设计的。
- 该花力气的地方是 **f 的形式**:座位内交互(实测 +0.024)、联合估计取代边际胜率、
  跨队克制。而不是换目标、也不是给队伍聚合套非线性(实测都没用)。

---

## 七、爬虫架构的一个坑(第一版 26204 局就跑干了)

两个接口的 steamId 可见性不一样,这决定了整个架构:

| 接口 | steamId | 用途 |
|---|---|---|
| `/matches/{id}` | **10 个人全给真实 id** | 只能靠它**发现玩家** |
| `/players/{id}/matches?page=N` | **只给被查询者本人,其余全是 −1** | 只能靠它**收对局** |

第一版全靠玩家翻页滚雪球,结果 26,204 局里出现的不重复真实 steamId **正好是最初的 1200 个种子**,
一个新玩家都没滚出来,队列耗尽提前收工。

正确架构是**交替**的:

```
种子 match_id(OpenDota) ──/matches/{id}──► 10 个玩家 ──/players/*/matches──► 30 局/请求
                              ↑ 1 请求换 10 人                  ↑ 队列见底就回去再换一批
```

实测效率:1200 个玩家 × 2.4 个请求 → 26,204 局去重后 = **每个玩家贡献约 22 局、
每个请求净得约 8.7 局**。要 30 万局需要约 1.4 万个玩家 = 1400 个种子 + 3.3 万个翻页请求,
单线程 2.5 req/s 约 **4 小时**。

种子要**跨整个补丁周期取**(实测 match_id 约 155 万/天,7.41d 覆盖
8.835e9 ~ 8.971e9),否则时间切分评估会失真。

---

## 八、L1 / L2 实测结果(15.5 万局)

去重后 7.41d **155,385 局**,时间切分 训练 132,077(其中末 10% 作验证早停)/ 测试 23,308。

| 层 | 内容 | AUC | logloss |
|---|---|---|---|
| — | 单体胜率相加 | 0.7005 | 0.6259 |
| L0 | + 两两组合 + 三连(windrun 表) | 0.7277 | 0.6067 |
| L0+ | + 17 个静态特征(windrun 全部 8 张表) | **0.7308** | **0.6044** |
| L1 | + 640 维主效应残差 | 0.7330 | 0.6026 |
| **L2** | **+ 座位内低秩嵌入协同(d=16)** | **0.7422** | **0.5952** |
| L2c | + 跨队克制 | 0.7360 | 0.6002 |

### windrun 的表已经被榨干

把他 8 张表全榨成 17 个每件物品的静态特征(单体胜率/选取率/**平均被选顺位**/顺位分歧度/
高分段溢价/7 个 KDA-GPM-XPM-伤害-治疗偏移/阿哈加成/魔晶加成/早期胜率/后期胜率/前后期斜率),
按队伍聚合后**只加 +0.003**。

系数排下来第二重要的是 **平均被选顺位**(−0.038)—— 早被抢的技能更强,
这是玩家群体的判断,和胜率不完全重合。其余(阿哈、KDA 偏移、前后期斜率)几乎全是 0。

### 覆盖率才是 windrun 的真短板

| 表 | 真实对局里查得到的比例 |
|---|---|
| 座位内两两组合 | **23.5%** |
| 座位内三连 | **7.5%** |
| 技能×载体适配 | 81.8% |

他的组合表是**排行榜不是全量表**:真实需要 C(640,2) ≈ 20 万个 pair,他只放前 7500 个。
所以自己训的意义不是"超过他的单体胜率"(13 万局做不到,他那是 78 万局算的),
而是**把查不到的 76.5% 补上** —— 这正是 L2 的 +0.0114 的来源。

### 跨队克制:证伪

```
克制关:  验证 logloss 0.5881(第 3 轮)→ 测试 0.7422 / 0.5952
克制开:  验证 logloss 0.6209(第 1 轮)→ 测试 0.7360 / 0.6002,之后一路炸到 0.84
```

每局 25×25 = 625 个跨队配对喂 2 万个参数,13 万局纯粹在记噪声。
而且 AD 双方是从**同一个 60 件池子**里选的,跨队配对与池内结构高度相关,信号本就被稀释。
**先不做。** 数据到 50 万局以上再复验。

### 踩过的坑

1. **协同项是嵌入的二次型,不能和线性部分共用学习率。**
   第一版 lr=0.02,|e| 一涨 syn = ½(|Σe|²−Σ|e|²) 就平方级放大,10 个座位加起来把 logit 顶出量程 ——
   AUC 还有 0.696 但 logloss 崩到 0.68(排序还对、概率全错)。我们要的恰恰是概率。
2. **L2 惩罚别加在已经除以 batch 的梯度上**,量纲差一个 batch size。改用解耦权重衰减。
3. **把 windrun 的线性部分冻结成固定偏置,FM 只学残差** —— 下界锁死在 L0,不可能训出比它更烂的东西。
4. **最优点在第 1~3 轮**(共 24 轮)—— 这是数据不够的信号,不是超参问题。

---

## 九、24 万局上的深挖(2026-08-29)

### 当前口径

| 层 | AUC | logloss |
|---|---|---|
| 底座:16 块 windrun 特征(39 列 + 20 列) | 0.7368 | 0.5997 |
| + 640 维主效应 + 640×32 座位嵌入 | **0.7520** | **0.5870** |
| + 高共现组合自由参数(阈值 300) | 0.7525 | 0.5866 |

校准:十分位偏差全部在 ±0.014 内。天辉截距 +0.1786,模拟器口径要扣掉。

### 特征逐块消融:除了 windrun 组合表,其余全是 0

```
拿掉 windrun 组合表(两两+三连)   logloss +0.0066   ← 唯一有分量的
拿掉 windrun 适配残差                    +0.0009
拿掉 大招与本体单列 / windrun17静态        +0.0005
其余 10 块(pick顺位/队内位次/载体属性/抢手度四象限/…)  全部 0.0000
```

**但这些特征在纯线性底座里确实有效**(比如"平均被选顺位"单加一列 +0.0044 AUC)——
一旦加上 640×32 嵌入就归零,因为手工特征只是嵌入能表达空间的**低维投影**。

> 规律:只有"windrun 用 78 万局算出来、我们 24 万局算不出来"的东西才有增量;
> 凡是能从原始对局学的,嵌入都已经学了。

### 协同层学到了什么(可解释性)

**本体×技能(适配)**,消融值 −0.0047,占协同层收益 28%:

```
最适配   衰退光环→水人 +0.255 / 锚击→水人 +0.209 / 盛宴→水人 +0.193
最不适配 衰退光环→米波 −0.178 / 衰退光环→哈斯卡 −0.157 / 锚击→米波 −0.147
最挑身板 衰退光环 0.433 / 锚击 0.355 / 盛宴 0.316   ← 全是光环和普攻强化
不挑身板 恐怖波动 / 异化 / 驯兽术 / 幻影冲锋  全部 0.04 ← 全是主动施放
```

**技能×技能**:

```
最合拍  锚击×熠熠生辉 +0.179 / 无敌斩×衰退光环 +0.175 / 勇气之霎×决斗 +0.116(7624 次)
最冲突  火焰风暴×衰退光环 −0.131 / 暗影之境×恐吓 −0.105 / 高射火炮×召唤飞弹 −0.100
```

### ⚠️ 关键限制:协同层实质只有两根轴

```
本体×技能 纯交互矩阵  第 1 主轴占 75.2%,前 2 轴 80.6%
技能×技能 纯交互矩阵  第 1 主轴占 76.5%,前 3 轴 84.6%
```

- 身板轴:身板好不好用 × 技能挑不挑身板(与"是否远程"相关仅 +0.191,
  与"英雄本体自身胜率"相关 +0.455)
- 流派轴:普攻流 ↔ 法系爆发("最挑搭档"的十个技能里七个的最不搭都是战神迅矛)

**而且这不是样本不足的假象** —— 学习曲线显示数据越多这根轴越集中:

```
训练局数     测试AUC   第1主轴占比(技能×技能)
 45,849     0.7395        35.0%
 91,699     0.7450        64.7%
183,398     0.7522        79.3%
```

低数据时的"多轴丰富"是随机初始化的噪声没被训掉。真实主导结构就是一根轴。

### 样本量在另一个层面仍然卡着

```
C(513,2) = 131,328 对技能组合
24 万局产生 1438 万次座位内共现 → 平均每对 110 次,中位 78 次
共现 ≥500 次的只占 0.85%
要让中位数达到 500 次需要约 154 万局
```

而 AUC 随数据的增长**完全没有收窄**(每翻倍稳定 +0.005~0.007),所以继续抓有明确收益。

### 高共现组合放自由参数:方向对,量太小

`协同_s = Σ_{i<j∈座位} [⟨e_i,e_j⟩ + θ_ij]`,θ 只对共现 ≥300 的 3883 对非零。

```
阈值 0   AUC 0.7520 / 0.5870
阈值 300 AUC 0.7525 / 0.5866   +0.0005(噪声级)
阈值 800 AUC 0.7520 / 0.5871   无
θ 与低秩内积相关 +0.327,|θ| 中位 0.0088 vs |低秩| 0.0197
```

但 θ 确实抓到了低秩表达不了的**机制层冲突**,例如
`卓尔游侠(本体) × 霜冻之箭` 低秩给 +0.0093(认为合拍),θ 纠正为 −0.0537
(两个攻击特效不叠加)。78 万局时共现中位数升到约 250、达标组合涨到两三万,再复验。

### 试过并判负的

| 方向 | 结果 |
|---|---|
| 中介法(draft→战力→胜负) | 同特征下 AUC 完全相等;高维下反而差 0.004~0.010 |
| 跨队克制(双线性) | 24 万局复验 0.7480 < 0.7508,仍然更差 |
| 座位层 DeepSets(MLP) | 0.7394 / 0.7486 / 0.7475,全部不如纯双线性 0.7504 |
| 队伍层非线性(饱和/截断) | 0.6970 / 0.6584,远差于线性 0.7024 |
| pick order 加权 | 系数 4.50→3.38 仅 1.33 倍;与简单加总相关 0.9977 |
| 强度分档(凸性) | 各档系数 3.1~4.6 无趋势,贡献 ≈ 4.3×(胜率−0.5) 线性 |

### 三个自己犯的错(留档)

1. **把桶系数除以桶内件数当"单件权重"** —— 造出假的"最强件是垫底的 10 倍",
   实际原始系数 3.5~5.1 基本是平的。特征已经是桶内求和,系数本身就是每单位权重。
2. **用玩具模型(只有单体胜率和)测单个特征的增量,据此判死** ——
   载体适配就是这么被我误判成"系数 0.000 完全没用"的,正确编码后消融值 −0.0047。
3. **从榜尾十个样本推规律** —— 看到最冲突的组合里有几对"同一个英雄的技能",
   差点断言"同英雄技能负协同";全量核实是 +0.00437(微正),把技能装回原主更是 +0.01076。
